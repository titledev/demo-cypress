name: Cypress E2E

on:
  workflow_call: {}
permissions:
  contents: write # สำหรับ push ไปยัง gh-pages branch
jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Nuxt project
        uses: actions/checkout@v4
        with:
          path: nuxt-app

      - name: Checkout Cypress project
        uses: actions/checkout@v4
        with:
          repository: titledev/demo-cypress
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          path: cypress-app

      - name: Install & run Nuxt
        run: |
          npm install
          npm run build
          npm run preview &
        working-directory: nuxt-app

      - name: Wait for Nuxt server
        run: npx wait-on http://localhost:3000

      - name: Install Cypress dependencies
        run: npm install
        working-directory: cypress-app

      # - name: Run Cypress E2E tests
      #   run: npx cypress run
      #   working-directory: cypress-app
      #   env:
      #     CYPRESS_baseUrl: http://localhost:3000

      # - name: Run Cypress E2E tests
      #   id: cypress
      #   run: |
      #     npx cypress run || echo "Cypress tests failed, continue to deploy"
      #   working-directory: cypress-app
      #   env:
      #     CYPRESS_baseUrl: http://localhost:3000

      # - name: Deploy mochawesome report to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v4

      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: "${{ github.workspace }}/cypress-app/cypress/reports"

      # - name: Deploy mochawesome report to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v4
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: "${{ github.workspace }}/cypress-app/cypress/reports"

      - name: Install gh-pages CLI
        run: npm install -g gh-pages

      - name: Run Cypress tests & Deploy reports
        run: |
          # Setup git info (required for gh-pages)
          # git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # git config --global user.name "github-actions[bot]"

          # Run Cypress and store result
          npx cypress run || true
          EXIT_CODE=$?

          # Show files before deploy
          echo "📂 Available reports:"
          find cypress/reports -type f -name "*.html" || echo "⚠️ No mochawesome reports found"

          # Deploy to GitHub Pages
          if [ -d "cypress/reports" ]; then
            gh-pages -d cypress/reports -b gh-pages -t true || echo "❌ Deploy failed"
          else
            echo "❌ cypress/reports folder not found — skipping deploy"
          fi

          # Exit with original test status
          exit $EXIT_CODE
        working-directory: cypress-app
        env:
          CYPRESS_baseUrl: http://localhost:3000
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
